<Page
    x:Class="SchedulerApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchedulerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <local:MainViewModel />
    </Page.DataContext>
    <Page.Resources>
        <local:NullToVisibilityConverter x:Key="nullToVisibilityConverter" />
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Content="Load" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Row="0" Command="{Binding ShowProjectsCmd}"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Projects}"></ListView>
        <TextBlock Grid.Row="2" Text="{Binding ErrorMsg}" FontStyle="Italic" Visibility="{Binding ErrorMsg, Converter={StaticResource nullToVisibilityConverter}}" />
    </Grid>
</Page>
